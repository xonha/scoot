# Editor / OS cruft
.DS_Store
*.swp
.vscode/

# Build / export artifacts (firmware) — add as the project grows
*.uf2
*.bin
*.hex
*.elf

# Ergogen build output (default output folder; we build elsewhere anyway)
output/

# Vendored external footprint library — fetched locally for the build, not tracked
# (see ergogen/README.md for how to fetch). Our own footprints (directly in
# ergogen/footprints/) stay tracked.
ergogen/footprints/ceoloide/
