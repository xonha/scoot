# Editor / OS cruft
.DS_Store
*.swp
.vscode/

# Build / export artifacts (firmware) — add as the project grows
*.uf2
*.bin
*.hex
*.elf

# Ergogen build output (default output folder; we build elsewhere anyway)
output/
